{
    "private": true,
    "scripts": {
        "dev": "vite", 
        "start": "php artisan serve --host=0.0.0.0 --port=$PORT",  
        "build": "vite build"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.2",
        "vite": "^4.0.0"
    }
}
